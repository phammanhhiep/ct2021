log:
  save_dir: "log"
  file_name: "evaluation"
  level: 10 # INFO=20, DEBUG=10

statistics: "experiments/stats/{}_three_stats.csv"

generated_image:
  save_dir: "experiments/images/"

model: 
  name: "20210511"
  load_dir: "experiments/models/"

head_pose_estimator:
  name: "hopenet_robust_alpha1"
  load_dir: "experiments/head_pose_estimator"

dataset: 
  evaluation: "datasets/ffhq/evaluation_data_list.dat"
  root_dir: "src/datasets/sample"